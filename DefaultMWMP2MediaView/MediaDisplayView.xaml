<UserControl x:Class="DefaultMWMP2MediaView.MediaDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DefaultMWMP2MediaView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Image Source="fond.png" Grid.ZIndex="0"/>
        <ContentControl Content="{Binding MediaElementObject}" Grid.ZIndex="1"/>
    </Grid>
</UserControl>
