<UserControl x:Class="DefaultMWMP2MediaView.MediaViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DefaultMWMP2MediaView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:MediaViewerModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:StaticViewModel}">
            <local:StaticView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:MediaDisplayViewModel}">
            <local:MediaDisplayView/>
        </DataTemplate>
    </UserControl.Resources>
        <ContentControl Content="{Binding Path=CurrentViewModel}"/>
</UserControl>
