<Window x:Class="MyWindowsMediaPlayerv2.MediaControlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:MyWindowsMediaPlayerv2.ViewModel"
        xmlns:myWindowsMediaPlayerv2="clr-namespace:MyWindowsMediaPlayerv2"
        mc:Ignorable="d"
        Title="MediaPlayer" Height="800" Width="800" Background="Black"
        WindowState="{Binding WindowState}" WindowStyle="{Binding WindowStyle}"
        WindowStartupLocation="CenterScreen"
        >
    <Window.DataContext>
        <viewModel:MainViewModel />
    </Window.DataContext>

    <ContentControl Content="{Binding Path=RootElement}"/>

</Window>
