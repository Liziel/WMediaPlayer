<UserControl x:Class="MediaLibrary.Audio.Pages.Page"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaLibrary.Audio.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Name="SideArea">
            <Button.Template>
                <ControlTemplate>
                    <Rectangle Fill="#111111" Opacity="0.7" Grid.Column="0"/>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Border Grid.Column="1" BorderThickness="0.6 0 0 0" BorderBrush="#555555" Background="#111111" Name="ContainerBorder">
            <Border.Effect>
                <DropShadowEffect Direction="200"/>
            </Border.Effect>
        </Border>
    </Grid>
</UserControl>
